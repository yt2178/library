<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication"
        tools:targetApi="31">

        <receiver
            android:name=".AlarmReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
            </intent-filter>
        </receiver>

        <!--  הגדרת מסך הפתיחה הראשי של האפליקציה (רק אחד צריך להיות LAUNCHER) -->
        <!--  אני מנחש ש-Welcome הוא מסך הפתיחה, שנה אותו אם זה PlanActivity או MainActivity -->
        <activity
            android:name=".Welcome"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--  רישום שאר ה-Activities באפליקציה -->
        <activity android:name=".PlanActivity" android:exported="false" />
        <activity android:name=".MainActivity" android:exported="false" />
        <activity android:name=".SelectMasechet" android:exported="false" />
        <activity android:name=".CongratulationsActivity" android:exported="false" />
        <activity android:name=".History" android:exported="false" />
        <activity android:name=".About" android:exported="false" />

        <!--  הפעילות 'Settings' צריכה להיות exported="true" אם היא מקבלת קבצים חיצוניים -->
        <activity
            android:name=".Settings"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:scheme="content"/>
                <data android:host="*"/>
                <data android:pathPattern=".*\\.shinantam"/>
                <data android:mimeType="*/*"/>
                <data android:scheme="file"/>
                <data android:host="*"/>
                <data android:pathPattern=".*\\.shinantam"/>
                <data android:mimeType="*/*"/>
            </intent-filter>
        </activity>

        <!--  הגדרות הווידג'טים (נשאר ללא שינוי) -->
        <receiver
            android:name=".WidgetProvider"
            android:exported="true"
            android:excludeFromRecents="true"
            android:label="@string/widget_full_description">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data android:name="android.appwidget.provider" android:resource="@xml/hebrew_widget_info" />
        </receiver>
        <activity
            android:name=".WidgetConfigActivity"
            android:exported="true"
            android:theme="@style/Widget.Configure.Dialog.Theme">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_CONFIGURE"/>
            </intent-filter>
        </activity>

        <receiver
            android:name=".WidgetCompactProvider"
            android:exported="true"
            android:excludeFromRecents="true"
            android:label="@string/widget_compact_description">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data android:name="android.appwidget.provider" android:resource="@xml/compact_widget_info" />
        </receiver>
        <activity
            android:name=".WidgetCompactConfigActivity"
            android:exported="true"
            android:theme="@style/Widget.Configure.Dialog.Theme">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_CONFIGURE"/>
            </intent-filter>
        </activity>

    </application>
</manifest>